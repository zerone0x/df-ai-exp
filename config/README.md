# Configuration Files

This directory stores local runtime configuration that is not committed to git.

- `df_root.txt` (ignored): absolute path to the Dwarf Fortress + DFHack install.

Create `df_root.txt` if you prefer file-based configuration instead of exporting
`DF_ROOT` in your shell environment.
